[libdefaults]
default_realm = {{ sssd_domain }}
default_ccache_name = %{TEMP}/krb5cc_%{username}

[realms]
{{ sssd_domain }} = {
    kdc = {{ sssd_kdc }}
    admin_server = {{ sssd_admin_server }}
}

[domain_realm]
.{{ sssd_domain | lower }} = {{ sssd_domain }}
{{ sssd_domain | lower }} = {{ sssd_domain }}
